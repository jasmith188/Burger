<h1 class ="top">EAT-DA-BURGER!</h1>
<i class="fas fa-hamburger"></i>
<div class="eaten-burger">
<h2>Burgers ready to eat!!! Get it while its hot!!!</h2>

<ul>
  {{#each burgers}}
    {{#unless devoured}}
      {{> burgers/burger-block devoured=true}}
    {{/unless}}
  {{/each}}
</ul>
</div>
<div class="not-eaten-burger">
<h2>These burgers are Great! Who is the Chef here??</h2>

<ol>
  {{#each burgers}}
    {{#if devoured}}
      {{> burgers/burger-block devoured=false}}
    {{/if}}
  {{/each}}
</ol>
</div>


<div>
<h2>Add a Burger</h2>
<form class="create-form">
<div class="form-group">
    <label for="bu">Burger Name:</label>
    <input type="text" id="bu" name="burger_name">
  </div>

  <button type="submit">Choose your Burger</button>
</form>
</div>

